name: Deploy Delivery Frontend

on:
  push:
    branches:
      - main
      - master
      - jaisaldev

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build Next.js project
        run: pnpm run build

      - name: Verify export folder
        run: ls -la out

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Upload build to S3
        run: |
          CLEAN_BUCKET=$(echo "${{ secrets.AWS_S3_BUCKET }}" | xargs)
          aws s3 sync out s3://$CLEAN_BUCKET --delete

      - name: Invalidate CloudFront cache
        run: |
          CLEAN_CF_ID=$(echo "${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}" | xargs)
          aws cloudfront create-invalidation \
            --distribution-id $CLEAN_CF_ID \
            --paths "/*"